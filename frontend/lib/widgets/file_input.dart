import 'package:flutter/widgets.dart';

class FileInput extends StatelessWidget {
  const FileInput({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('File Input');
  }
}
